version: 0.2

phases:
  install:
    commands:
      - echo "🔧 Installing dependencies..."
      - apt-get update -y
      - apt-get install -y unzip
  build:
    commands:
      - echo "🚀 Building project..."
      - ls -la
      - echo "✅ Build complete!"

artifacts:
  files:
    - '**/*'

